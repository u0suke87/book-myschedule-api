#book-myschedule-api document

##まず初めに
このアプリケーションではClient CredentialsのOAuth2.0の認証フローを実装しています。このアプリケーションを使う場合にはGoogle APIsにおいて、OAuthクライアントIDをjson形式で別途作成し、client-secret.jsonとしてこのアプリケーションのホームディレクトリに置いて下さい。

##今後のTo do
###追加したスケジュールの修正,削除の機能を実装
- bookingした本人のeventのみを編集できるようにするため、スケジュール追加時点でその時刻を元に編集用のkeyを発行し、ブラウザに表示してユーザー側で保管してもらう。サーバー側では発行したkeyをeventと結びつけてDBに保存して運用する。開始時刻が過ぎたeventに関しては随時DBから該当keyを削除する。
- (2019/06/18 追記) Relationalである必要はないが、ビックデータを取り扱う訳ではなく高速化の必要もないので今回はMariaDB(RDB)を用いる。

###既に予定が入っている時間には予定を入れられない機能を実装
- 現状、ダブルブッキングが可能なシステムなので、既存のeventを読み込み、indexページで既に予定が入っている日時に追加で予定を入れられないように動的な表示を行う。具体的に使用するフロント技術は未定。

###aws EC2にデプロイ
- 自身の勉強のため、dockerでアプリケーションをコンテナ化してEC2 or heroku 上で動かす想定。

###UI,CSS等見た目の修正

